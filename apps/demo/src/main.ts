// Rime demo app — placeholder entry.
// Built out later into the full end-user UX showcase (local store,
// onImageUpload stub, theme showcase). For now it proves the app builds + runs.

const app = document.querySelector<HTMLElement>("#app");
if (app) {
  app.textContent = "Rime demo — coming soon.";
}
