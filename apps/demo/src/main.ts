// Enveloppe demo app — placeholder entry.
// ENV-92 builds this into the full end-user UX showcase (local store,
// onImageUpload stub, theme showcase). For now it proves the app builds + runs.

const app = document.querySelector<HTMLElement>("#app");
if (app) {
  app.textContent = "Enveloppe demo — coming soon (ENV-92).";
}
